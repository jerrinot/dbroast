<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>The DB Grill — Daily Database Roasts</title>
  <meta name="theme-color" content="#101214" />
  <link rel="icon" href="/assets/favicon.svg" type="image/svg+xml" />
  <link rel="alternate" type="application/rss+xml" href="/feed.xml" title="DB Grill RSS feed" />
  <link rel="stylesheet" href="/assets/styles.css" />
</head>
<body>
  <div class="smoke-layer smoke-1" aria-hidden="true"></div>
  <div class="smoke-layer smoke-2" aria-hidden="true"></div>
  <div class="container">
    <header class="site-header">
      <div class="brand">
        <a href="{{ '/' | url }}" class="brand-logo" aria-label="DB Grill Home">
          <img src="/assets/logo.svg" alt="DB Grill logo" width="40" height="40" style="width:40px;height:40px;" />
        </a>
        <div>
          <h1 class="brand-title">The DB Grill</h1>
          <p class="brand-subtitle">Where database blog posts get flame-broiled to perfection</p>
        </div>
        <a class="rss-link" href="/feed.xml">RSS</a>
      </div>
    </header>

    {{ content | safe }}

    <footer class="site-footer">
      <p>Lovingly crafted with equal parts spite and stored procedures by <a href="https://twitter.com/jerrinot">@jerrinot</a></p>
      <p style="margin-top:6px">No databases were harmed in the making of these roasts. Their egos, however... 🔥</p>
    </footer>
  </div>

  <script>
    function toggleRoast(roastId, button) {
      const roastElement = document.getElementById(roastId);
      const isCollapsed = roastElement.classList.contains('collapsed');
      if (isCollapsed) {
        roastElement.classList.remove('collapsed');
        button.textContent = 'Show less ↑';
      } else {
        roastElement.classList.add('collapsed');
        button.textContent = 'Read full roast →';
        const articleContainer = roastElement.closest('.roast-item');
        const articleTitle = articleContainer && articleContainer.querySelector('.roast-title');
        if (articleTitle) articleTitle.scrollIntoView({ behavior: 'smooth', block: 'start' });
      }
    }
  </script>
</body>
</html>
